변수의 형식은 다양하다. 정수인 int, 유리수인 float, 문자인 str, oX값을 가지는 bool, 리스트인 list 등 수없이 많은 형식을 가지고 있다.
변수를 선언할 때 이를 구분할 필요는 없으나 연산과 출력 부분에서 주의해야 한다.

int : 1, 2, 3, 4, 5
float : 1.4, 0.82734, 3.141592
str : '밥은 맛있다.', "사과", '1', "weyufgqsduf"
bool : True, False
list : [], [1, 2, 3, 4, 5], [0, 1, '사과', 5.43, True, [1, 3.5], 1234]


* input에서 입력받은 값은 전부 str형식이다.
* 다른 형식을 가진 변수끼리는 계산할 수 없다.


형식 변환 방법
int로 바꾸고자 한다면 : int(바꾸고자 하는 값)
float로 바꾸고자 한다면 : float(바꾸고자 하는 값)
.
.
.
